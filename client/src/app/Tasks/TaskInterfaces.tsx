export interface TaskProps {
    description: string;
    checked: boolean;
    id: string;
}

export interface TaskInterface {
    description: string;
    id: string;
    type: string;
    checked: boolean;
}
