import { makeStyles } from "@material-ui/core";

const ToDoListStyle = makeStyles(() => ({
  wrapperTasks: {
    padding: "1rem 0rem",
  },
  wrapperTask: {
    paddingBottom: "1rem",
  },
}));

export default ToDoListStyle;
